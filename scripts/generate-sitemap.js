import {
  generateImageSitemap,
  generateStaticSitemap,
} from "./sitemap-utils.js";

// Generate static files sitemap
generateStaticSitemap();

// If you have dynamic images, generate image sitemap
// generateImageSitemap();
